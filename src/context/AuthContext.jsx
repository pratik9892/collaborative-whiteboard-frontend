import React from 'react';

/**
 * AuthContext - The actual context object
 * This file just creates the context
 * AuthProvider (next file) will provide the values
 */

export const AuthContext = React.createContext(undefined);
